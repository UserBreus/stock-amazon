async function cleanGhost() {
    try {
        const fetch = require('node-fetch'); // we use node fetch in powershell context if bypassing
        // wait we can just run this using the standard node fetch structure
    } catch(e) {}
}
